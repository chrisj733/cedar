

TAG=dev
REGISTRY=${DOCKER_REGISTRY:-harbor.unx.sas.com}

IMAGE_PATH="adx/cedar"

docker build -t ${REGISTRY}/${IMAGE_PATH}:${TAG} .
docker push ${REGISTRY}/${IMAGE_PATH}:$TAG

TAG=test
REGISTRY=${DOCKER_REGISTRY:-harbor.unx.sas.com}

IMAGE_PATH="adx/cedar"

docker build -t ${REGISTRY}/${IMAGE_PATH}:${TAG} .
docker push ${REGISTRY}/${IMAGE_PATH}:$TAG


TAG=prod
REGISTRY=${DOCKER_REGISTRY:-harbor.unx.sas.com}

IMAGE_PATH="adx/cedar"

docker build -t ${REGISTRY}/${IMAGE_PATH}:${TAG} .
docker push ${REGISTRY}/${IMAGE_PATH}:$TAG


